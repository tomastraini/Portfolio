# dependencies
/node_modules

# build output
# NOTE: docs/ is intentionally NOT ignored — GitHub Pages serves the site
# from that folder, so the built assets are committed on purpose.
/build
/dist

# test artefacts
/test-results
/playwright-report
/blob-report
/playwright/.cache
/coverage

# typescript incremental build info
*.tsbuildinfo

# misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local
.vite

npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env variables
IA_BackEnd/app/.env
IA_BackEnd/**/__pycache__/
